{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "gcc",
            "command": "/usr/bin/gcc-9",
            "args": [
				"-g",
				"-std=c++17",
				"-Wall",
				"-m64",

				"${workspaceFolder}/font_draw/src/main.cpp",
				"${workspaceFolder}/external/glad/glad.c",
				"${workspaceFolder}/lib/ogl/shader.cpp",
				"-o",
				"${workspaceFolder}/hellogl",

				"-I/usr/include/SDL2/"

				"-lSDL2",
				"-lGL",
				//"-lstdc++fs", //filesysstem??
				//"-lpthread",
				"-lstdc++", // for chrono, timenow? needed for gcc, clang without

            ],
            "options": {
                "cwd": "/usr/bin"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Generated task by Debugger"
        }
    ],
    "version": "2.0.0"
}